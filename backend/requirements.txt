# =============================================================================
# Agentic RAG Research Assistant — Dependencies
# =============================================================================
# Using minimum-version constraints instead of exact pins because Python 3.14
# is very new and many packages only have compatible wheels in their latest
# releases. Pip will resolve the newest compatible versions automatically.
# =============================================================================

# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
pydantic>=2.11.0
python-dotenv>=1.0.0

# LangChain + LangGraph
langchain>=0.3.20
langgraph>=0.4.0
langchain-openai>=0.3.15
langchain-google-genai>=2.1.0
langchain-community>=0.3.20
langchain-huggingface>=0.1.2

# Vector Store
chromadb>=1.0.0

# Document Parsing
pdfplumber>=0.11.0
python-docx>=1.1.0

# Embeddings (local, free)
sentence-transformers>=4.0.0

# Evaluation (install separately in Session 9 if needed)
# ragas>=0.4.0

# Observability
langsmith>=0.3.40

# Utilities
httpx>=0.28.0
